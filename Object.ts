// methods used in the type script for Objects

// Mainly
// Looping through Object
// Empty Object / Finding object

// keys values

// What is object null and undefined

function showFruits() {
  const fruits = [
    {
      id: 1,
      name: "apple",
    },
    {
      id: 2,
      name: "banana",
    },
    {
      id: 3,
      name: "oragne",
    },
  ];
}

let showFruitName = () => {
  console.log("I am writing the fruits name ");
};

(a) => a + 1;

// function expression
let x = function (x, y) {
  return x * y;
};

// using arrow functions
let y = (x, y) => x * y;

const getSum = (x, y) => x + y;


getSum(10,11)


